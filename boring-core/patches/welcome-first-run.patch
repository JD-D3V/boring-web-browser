--- a/chrome/browser/chrome_browser_main.cc
+++ b/chrome/browser/chrome_browser_main.cc
@@ -1277,7 +1277,9 @@
   if (first_run::IsChromeFirstRun()) {
     if (!base::CommandLine::ForCurrentProcess()->HasSwitch(switches::kApp) &&
         !base::CommandLine::ForCurrentProcess()->HasSwitch(switches::kAppId)) {
-      browser_creator_->AddFirstRunTabs({GURL("chrome://ungoogled-first-run")});
+      // boring: our own welcome page instead of ungoogled-chromium's,
+      // which is written for people who build browsers.
+      browser_creator_->AddFirstRunTabs({GURL("chrome://boring-welcome")});
       browser_creator_->AddFirstRunTabs(master_prefs_->new_tabs);
     }
   }

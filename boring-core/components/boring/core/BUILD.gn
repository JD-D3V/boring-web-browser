# Copyright 2026 boring. BSD style license.

static_library("core") {
  sources = [
    "boring_capabilities.h",
    "boring_prefs.cc",
    "boring_prefs.h",
  ]
  deps = [
    "//base",
    "//components/pref_registry",
    "//components/prefs",
    "//net",
    "//url",
  ]

  # Not code, but it has to land next to chrome.exe, and everything we
  # build already reaches this target.
  data_deps = [ "//components/boring/notices" ]
}
